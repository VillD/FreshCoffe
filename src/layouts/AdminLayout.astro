---
import Logo from "@/assets/logo.svg";
import Layout from "./Layout.astro";
import AdminSidebar from "@/components/admin/AdminSidebar";
import LogoutButton from "@/components/ui/LogoutButton.astro";
interface Props {
  title: string;
}
const { title } = Astro.props;
---
<Layout title={`Panel de Administración: ${title}`}>
  <div class="md:flex">
    <aside
      class="md:w-[320px] md:h-screen bg-gray-900 border-r-2 border-gray-200 flex flex-col"
    >
      <div class="flex justify-center border-b-2 border-gray-200 p-5">
        <Logo />
      </div>
      <div class="p-5 flex-1 flex flex-col justify-between">
        <AdminSidebar client:only="react"/>
        <LogoutButton/>
      </div>
    </aside>
    <main class="md:flex-1 md:h-screen md:overflow-y-scroll p-5 bg-gray-900">
      <h1 class="text-2xl my-10 text-white" set:html={title}></h1>
      <slot />
    </main>
  </div>
</Layout>